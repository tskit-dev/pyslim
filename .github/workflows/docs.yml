
name: Docs
on:
  pull_request:
  push:
    branches: [main]
    tags:
      - '*'
  merge_group:
jobs:
  Docs:
    permissions:
      contents: read
    uses: tskit-dev/.github/.github/workflows/docs.yml@v19
    with:
      install-slim: true
